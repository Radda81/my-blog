# CLAUDE.md

이 파일은 이 저장소에서 작업하는 Claude Code(claude.ai/code)를 위한 가이드입니다.

## 프로젝트 개요

마크다운 파일을 읽어 정적 블로그 웹사이트로 변환하는 프로젝트. 프레임워크나 빌드 도구 없이 순수 HTML, CSS, JavaScript만으로 구현한다.

## 핵심 원칙

- **프레임워크 금지**: React, Vue, Next.js 등 어떤 프론트엔드 프레임워크도 사용하지 않는다. 번들러(Webpack, Vite 등)나 트랜스파일러도 사용하지 않는다.
- **의존성 최소화**: npm 패키지가 필요하더라도 런타임(브라우저)에는 순수 JS만 전달되어야 한다. 마크다운 파서 정도는 예외적으로 허용하되, 가능하면 직접 구현하거나 단일 파일 라이브러리를 vendor로 포함한다.
- **정적 사이트**: 서버 사이드 렌더링이나 백엔드 없이 정적 파일만으로 동작해야 한다(브라우저에서 fetch로 .md 파일을 읽거나, 빌드 시점에 HTML로 미리 변환하는 방식 중 하나를 프로젝트 진행하며 결정).

## 디자인 요구사항

- **깔끔하고 읽기 좋은 타이포그래피**: 본문 가독성을 최우선으로 한다 (적절한 줄 간격, 본문 폭 제한(예: max-width 65-75ch), 여백).
- **다크 모드 지원**: `prefers-color-scheme` 미디어 쿼리를 기본으로 하고, 사용자가 수동으로 토글할 수 있는 스위치도 제공한다. 선택한 테마는 `localStorage`에 저장해 새로고침 후에도 유지한다.
- **반응형/모바일 대응**: 모바일 화면에서도 레이아웃이 깨지지 않고 터치 사용에 불편함이 없어야 한다. 미디어 쿼리로 브레이크포인트를 관리한다.

## 예상 구조

프로젝트가 아직 초기 단계이므로 구조는 진행하면서 확정한다. 대략적인 방향:

```
/posts/          마크다운 원본 파일 (.md)
/css/            스타일시트 (예: style.css, theme 관련 변수)
/js/             마크다운 파싱, 라우팅, 다크모드 토글 등의 스크립트
/index.html      목록/홈 페이지
```

## 개발 시 참고사항

- 코드에 프레임워크 관련 패턴(JSX, 컴포넌트 클래스 등)을 도입하지 말 것 — 순수 DOM API(`document.createElement`, `innerHTML` 등)를 사용한다.
- CSS는 커스텀 프로퍼티(`--variable`)로 라이트/다크 테마 색상을 관리하면 다크모드 전환이 단순해진다.
- 새 마크다운 파일을 추가하는 것만으로 블로그에 글이 반영되도록 목표한다 (파일 목록을 수동으로 관리해야 하는지, 빌드 스크립트로 자동 인덱싱할지는 초기 구현 시 결정 필요).
